# Invalid coarse center returns to search

User authorized correcting repeated coarse_center_error_invalid stalls on vehicle 1. Invalid center values now stop for candidate_confirmation_timeout_sec (default .8s), then release the world/entity/nearest locks, clear recheck state, and return to existing search. candidate_retry_cooldown_sec (default 1s) prevents the same candidate immediately stopping search. Valid center values and alignment reset/entry clear the invalid timer. Existing concurrent dock_front_distance_hold changes were preserved; only this fix was patched on the vehicle after checking its source hash.

Local arrival/isolation/new regression suite: 289 passed, no ROS init or Node creation. Vehicle source and installed-module focused tests: 7 passed each. Normal copy-based colcon build --base-paths src --packages-select auto_dock succeeded (setuptools deprecation warning). Source and installed SHA256: 59620faac93fade61d260f4abea41f4535f0793399dc8364fd08145f1391c4bf. Installed package, backup, and temporary test directory contain zero symlinks.

Vehicle backup: /home/ubuntu/ros2_ws/backups/coarse_invalid_20260907T081535Z (previous source and installed node files). No node, controller, GUI, launch, stopper, or drive command was executed. User must restart auto_dock for the installed change to apply; physical search behavior remains unverified. Y PLACE integration remains preparation only.

## Follow-up: recorded visual-wait stall

Recording teleop_20260907_081915 (21.297s wall time; MP4 is 85 frames at nominal 15fps, so use frame overlay/metadata for actual elapsed time) showed nearest_center_recheck_waiting_visual after coarse_center_error_invalid. This earlier-return branch bypassed the previous timeout. Controls after elapsed 6s contain 647 commands, all zero. Added shared wait_for_coarse_visual for invalid center and the missing locked visual before center recheck is scheduled; the two failure modes share one deadline. Added direct missing-visual and invalid-to-missing transition regressions. Local 291 tests passed; vehicle source/installed focused tests 9 each passed. Normal copy build succeeded, source/installed SHA256 e6d5f1047e06438763a973008cdf7123fd33e0c6b08c82015f7d8f87afa5b7e0; installed/backup/test directory symlinks zero. Backup /home/ubuntu/ros2_ws/backups/coarse_visual_20260907T082303Z. User restart required; runtime not executed.

User also identified the skipped LEFT DIAMOND pallet (not the star pallets). Offline replay of recorded inference through nearest_product_candidate with default thresholds returned no candidate for entity 28 at elapsed .5–1.49s: PnP yaw about -74deg, later depth yaw +30.7deg. Config/default limits are 45deg PnP and 12deg depth. Partial pallet fallback rejects this pallet at image x=0. Search nevertheless used its +30.7deg yaw through any_front_entity_yaw_error (allows up to 45deg), emitted +.35rad/s, then reverted to IMU correction -.35rad/s when it vanished. First accepted candidate appears at 2.51s with a changed matrix; entities alternate 2/30. This establishes selection/guidance inconsistency, not proof that measured yaw was physically correct or that speed alone caused the miss. No change to yaw filters, search speed, or candidate selection was made in this follow-up. Existing inventory state was not reconstructed in the offline selection replay.

services:
  gazebo-server:
    devices:
      - /dev/dri:/dev/dri
    environment:
      LIBGL_ALWAYS_SOFTWARE: "0"
    group_add:
      - "${RENDER_GID:?RENDER_GID must be a numeric render node GID}"

#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PROJECT_ROOT="$(cd "$SCRIPT_DIR/../.." && pwd)"
PYTHONPATH="$PROJECT_ROOT/llm-wiki-core${PYTHONPATH:+:$PYTHONPATH}" exec python3 -m llm_wiki_core.core "$@"


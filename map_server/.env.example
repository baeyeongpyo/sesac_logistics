# Repository-local directory holding map_0825.yaml and map_0825.pgm.
MAP_DIRECTORY=./maps

# Path inside the read-only /maps mount. Change only when selecting another map.
MAP_YAML=/maps/map_0825.yaml
ROS_DOMAIN_ID=225
MAP_USE_SIM_TIME=false
MAP_SERVER_IMAGE=mentorpi-map-server:humble

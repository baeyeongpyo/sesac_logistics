# Source

`hiwonder_mecanum_forklift.urdf` is a Foxglove-compatible visual URDF derived
from `models/hiwonder_mecanum_forklift/model.sdf` in
`agent/prj-araneum-canonical` at commit `770b1a842fcdce8f82af56f658f3c21d503d660c`.

The three STL files in `meshes/mentorpi/` are copied unchanged from that
model. Their upstream provenance and license note are retained in
`meshes/mentorpi/SOURCE.txt`.

# Foxglove 3D Panel 자산 루트다. 내부에 hiwonder_mecanum_forklift URDF와 mesh를 둔다.
ASSET_DIRECTORY=./assets

# 신뢰된 LAN의 Foxglove 클라이언트만 접근할 수 있도록 방화벽을 설정한다.
ASSET_BIND_ADDRESS=0.0.0.0
ASSET_PORT=8081

# 배포 환경에 맞는 이미지 태그가 필요할 때만 변경한다.
FOXGLOVE_ASSET_SERVER_IMAGE=mentorpi-foxglove-asset-server:latest
